#include "iostream"

using namespace std;

int main() {

    string num;
    cin >> num;

    int len = 0;

    while (len != num.length()-2) {
        if ((int)num[len] != 0) {
            
        }
    }
}